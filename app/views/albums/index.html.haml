%h1 Listing albums

%table
  %tr
    %th Movie
    %th Description
    %th Title
    %th Actions

  - @albums.each do |album|
    %tr
      %td= image_tag album.photo(:medium)
      %td= album.description
      %td= album.name
      %td
        = link_to 'Show', album
        = link_to 'Edit', edit_album_path(album)
        = link_to 'Destroy', album, :confirm => 'Are you sure?', :method => :delete

%br

= link_to 'New Album', new_album_path
